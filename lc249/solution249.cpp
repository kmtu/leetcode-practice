#include "solution249.h"

using std::vector;
using std::string;

vector<vector<string>> Solution249::groupStrings(input_type& strings) {
    return output_type{};
}

vector<vector<string>> Solution249::run(input_type strings) {
    return groupStrings(strings);
}

